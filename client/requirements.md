## Packages
framer-motion | For complex page transitions, scroll animations, and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely
react-hook-form | Performant, flexible and extensible forms with easy-to-use validation
@hookform/resolvers | Validation resolvers for React Hook Form (using Zod)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
